<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="tmoney.gbi.bms.mapper.CommonInsertMapper">

    <insert id="insertLocation" parameterType="encryptedLocationDto">
        INSERT INTO ENCRYPTED_LOCATION (
            device_id,
            vehicle_id,
            encrypted_latitude,
            encrypted_longitude,
            occur_dt
        )
        VALUES (
                   #{deviceId},
                   #{vehicleId},
                   #{encryptedLatitude},
                   #{encryptedLongitude},
                   #{occurDt}
               )
    </insert>

    <select id="readLocation" parameterType="String">
        select
            device_id,
            vehicle_id,
            encrypted_latitude,
            encrypted_longitude,
            occur_dt
            from ENCRYPTED_LOCATION
        where device_id = #{deviceId}

    </select>

</mapper>
